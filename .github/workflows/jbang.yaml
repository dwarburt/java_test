name: jbang

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Setup JBang
        uses: jbangdev/setup-jbang@main
      - name: runner
        run: |
          jbang hello.java